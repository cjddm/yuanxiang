<!--miniprogram/pages/collect/collect.wxml-->
<block wx:for="{{dataList}}" wx:key="index">
	<view class="f-view" bindtap="goDetail" data-item="{{item}}">
		<text class="text-one">{{item.from}}</text>
		<text class="text-two">{{item.msg}}</text>
	</view>
</block>